﻿@model GameStore.WebUI.ViewModels.PublisherAddUpdateViewModel

@{
    ViewBag.Title = GlobalRes.NewPublisher;
}

<div class="well">
    @using (Html.BeginForm("New", "Publisher", FormMethod.Post, new {@class = "form-horizontal"}))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary()

        <fieldset>
            <legend>@ViewBag.Title</legend>

            @Html.EditorForModel()

            <div class="form-group">
                <div class="col-lg-10 col-lg-offset-2">
                    @Html.ActionLink(GlobalRes.ToList, "Get", "Game", null, new {@class = "btn btn-default"})
                    <button type="submit" class="btn btn-primary">@GlobalRes.Submit</button>
                </div>
            </div>
        </fieldset>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}